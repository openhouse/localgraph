{
  "schemaVersion": 1,
  "suite": "instagram-messages",
  "description": "Deterministic acquisition and integrity gates for private Instagram message imports.",
  "cases": [
    {
      "id": "test_drive.DrivePullTests.test_daily_import_prefers_configured_drive_api_when_explicit_local_source_is_empty",
      "criterion": "A configured read-only Drive API pull overrides an explicit Drive Desktop path that has not materialized message files."
    },
    {
      "id": "test_drive.DrivePullTests.test_configured_sync_selects_only_the_latest_export_from_a_drive_container",
      "criterion": "A stable Drive container follows dated Meta stream rollover while downloading only the newest Instagram export and no unrelated Drive trees."
    },
    {
      "id": "test_drive.DrivePullTests.test_successful_drive_pull_advances_the_current_instagram_mirror",
      "criterion": "A completed authenticated pull atomically advances the stable local Instagram mirror and records observable freshness state."
    },
    {
      "id": "test_drive.DrivePullTests.test_failed_drive_pull_keeps_the_last_known_good_current_mirror",
      "criterion": "A failed or partial provider pull cannot replace the last completed local Instagram mirror."
    },
    {
      "id": "test_automation.AutomationTests.test_instagram_sync_launchagent_runs_hourly_and_at_login",
      "criterion": "The Instagram-only macOS LaunchAgent checks hourly and at login without pinning an online-only Drive Desktop path."
    },
    {
      "id": "test_automation.AutomationTests.test_exact_export_root_ignores_nested_spotlight_duplicates",
      "criterion": "macOS Spotlight results cannot split one exact export snapshot into duplicate nested import roots."
    },
    {
      "id": "test_ingest.IngestTests.test_overlapping_instagram_exports_deduplicate_messages_after_repagination",
      "criterion": "The same logical message remains one canonical row when overlapping exports change file pagination or array order."
    },
    {
      "id": "test_drive.DrivePullTests.test_daily_import_uses_existing_private_cache_when_drive_pull_fails",
      "criterion": "A previously verified private cache remains usable when the provider pull is temporarily unavailable."
    },
    {
      "id": "test_ingest.IngestTests.test_import_instagram_messages_people_group_and_media",
      "criterion": "Instagram messages, participants, groups, media references, transcripts, and person capsules render from canonical state."
    },
    {
      "id": "test_cli.CliTests.test_init_accepts_repository_eval_and_script_entries",
      "criterion": "Repository-owned eval and script entries remain compatible with Localgraph workspace validation."
    }
  ]
}
